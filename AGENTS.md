# 高中数学专题文档生成、数学建模与数学问题解决

面向"刚学完必修课、基础较弱"的学生，同时满足数学建模竞赛论文的官方格式要求。所有数学内容须与**人教版A版教材**对齐，讲清来龙去脉，难度循序渐进，且保证每一步推导正确。

## 一、数学专题 Word 文档生成（.docx）

### 标准文档结构
```
标题页
├─ 知识梳理
│   ├─ 知识网络（层级树：类别 → 条目）
│   ├─ 核心概念精讲（从定义讲起，逐条展开）
│   ├─ 重要公式一览（表格：名称|公式|说明）
│   ├─ 常见误区辨析（⚠常见错误 + ✓正确理解）
│   └─ 重点提示
└─ 每个专题（按教材章节拆分）
    └─ 每个考点
        ├─ 正文讲解
        ├─ 例题 1 道（完整步骤）
        ├─ 实战 5 道（带难度星级 + 答案）
        └─ 每专题末尾 1 个拓展（选学/背景知识）
```

### 内容规范
- 概念从定义讲起，例题逐步解答（① ② ③），答案含推导过程；例题编号全文档连续（【例题1】~【例题N】）。
- 实战难度用星级：★☆☆ 基础 / ★★☆ 中等 / ★★★ 拔高，每题都附答案；每考点=1例题+5实战。
- 几何题优先用正方体/长方体/坐标法（数字简单、可建系）；最终结果用"答：..."收尾。
- 生成后必须校验：例题编号连续、每考点实战数=5、答案齐全、无公式错漏。

### 题型与难度设计（增强）
- **分层出题**：每考点 5 题按 ★☆☆(2 题)→★★☆(2 题)→★★★(1 题) 递进，先巩固定义再训练技巧最后综合。
- **考点覆盖**：对照人教版A版教材每节的知识点清单逐项核对，不漏考点（如圆锥曲线须含定义/标准方程/几何性质/离心率/焦点三角形/第三定义/直线与曲线/焦点弦/距离最值等）。
- **变式设计**：同一考点提供"数字变式、条件变式、逆向变式"（已知结果求参数、已知条件反推类型）。
- **衔接高考**：题源尽量贴近近年高考真题的设问方式，注明所用方法（坐标法、待定系数法、设而不求、点差法）。

## 二、数学建模

### 2.1 建模竞赛论文官方标准（对齐全国大学生数学建模竞赛 CUMCM 格式规范）

论文按以下结构组织，**页数限制内**完成（常规正文+附录 20~25 页以内）：

| 顺序 | 模块 | 要求 |
|---|---|---|
| 1 | **摘要** | 独立成页，300 字左右（不超过一页）。概述：研究的问题→采用的核心模型/方法→关键结果与结论。忌写"本文分析了……"的空话，须给出"用了什么模型、得到什么数值/结论"。 |
| 2 | **关键词** | 3~5 个，反映模型方法与研究对象，逗号分隔。 |
| 3 | **问题重述** | 用自己的话复述赛题，提炼待求解目标，不抄原题。 |
| 4 | **问题分析** | 逐问分析：问题的本质、拟采用的思路、总体框架（可配合流程图）。 |
| 5 | **模型假设** | 逐条列出，条条可检验、可辩护；说明假设的合理性，不写无意义假设。 |
| 6 | **符号说明** | 表格：符号 / 含义 / 单位；首次出现处也应定义。 |
| 7 | **模型建立与求解** | 每个问题一个小节：建模依据→模型表达式→求解方法→数值结果。公式、图、表都要编号并正文引用（如式(1)、图3、表2）。 |
| 8 | **模型检验** | 误差分析、残差分析、灵敏度分析、与真实数据对比。 |
| 9 | **模型评价与推广** | 优点（具体）、缺点（如实）、推广方向（换数据/换领域）。 |
| 10 | **参考文献** | 采用 GB/T 7714 格式，正文按出现顺序 [1][2]… 标注。 |
| 11 | **附录** | 程序代码、中间数据、推导细节。代码须可运行、有注释、无绝对路径与密钥。 |

**格式硬性要求**：A4 纸；正文宋体/仿宋（LaTeX 用 ctex）；标题黑体加粗；公式单独成行右对齐编号；图表有编号与题注；摘要前不加序号；全文用语书面化、逻辑连贯；禁止口语化、禁止出现"本人、我"等。

### 2.2 建模流程
1. **问题重述**：提炼目标与约束。
2. **模型假设**：列出关键假设。
3. **符号说明**：表格化。
4. **模型建立**：给出数学表达式/算法框架与依据。
5. **模型求解**：解析推导或数值求解（推荐 Python：numpy/scipy/sympy/pandas/matplotlib）。
6. **结果分析**：代入数据检验、灵敏度、误差。
7. **模型评价**：优点、局限、改进。

### 2.3 常用模型与算法速查（按问题类型）
- **优化类**：线性规划/整数规划（scipy.optimize、pulp）、图论最短路/最大流、动态规划。
- **预测类**：时间序列（ARIMA）、回归（线性/多项式）、灰色预测 GM(1,1)、机器学习（线性回归、决策树）。
- **评价类**：层次分析法 AHP、熵权法、TOPSIS、模糊综合评价、主成分分析 PCA。
- **统计/数据处理**：假设检验、方差分析、聚类（K-means）、插值与拟合。
- **物理/机理类**：微分方程模型（常微分初值问题，scipy.integrate.solve_ivp）、数值方法。
- 每个模型给出：适用场景、数学表达、求解工具、输出形式。

### 2.4 检验与灵敏度
- 用实际/模拟数据验证模型输出合理性；对关键参数做 ±10%、±20% 扰动，观察结果变化并解释。
- 误差指标：MSE、RMSE、MAE、R²，需在论文中给出并解释。

### 2.5 参考文献格式（GB/T 7714 示例）
```
[1] 作者. 题名[J]. 刊名, 年, 卷(期): 起止页.
[2] 作者. 书名[M]. 出版地: 出版社, 年.
[3] 作者. 题名[EB/OL]. (更新日期)[引用日期]. URL.
```

### 2.6 附录代码规范
- 注释解释思路；避免绝对路径、明文密钥；图表输出存 PNG；运行结果与论文数值一致。

## 三、LaTeX 生成规范

### 中文数学论文/文档
```latex
\documentclass[12pt]{ctexart}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{geometry, graphicx, booktabs}
\geometry{a4paper, margin=2.5cm}
```
- 中文正文用 ctex 系；行内公式 `$...$`，独立公式 `\[...\]`、`equation`、`align`；公式编号与正文引用（`\label`/`\eqref`）。
- 图表用 `figure`/`table` 环境 + `\caption`，正文 `\ref` 引用。
- 定理/例题用 `\newtheorem`；表格用 `booktabs`；图形用 tikz 或插入 PNG。

### 公式风格
- 行内 `$x_1^2$`；多行 `align`；分数 `\frac{a}{b}`；根式 `\sqrt{}`；希腊字母 `\alpha,\theta,\lambda,\pi`。
- 向量 `\vec{a}` 或 `\mathbf{a}`；集合 `\in,\subseteq,\cap,\cup`；逻辑 `\iff,\Rightarrow`；求和 `\sum`、积分 `\int`。
- 完成后编译验证（TeX Live 等），确保无编译错误。

## 四、数学问题解答

- 先明确已知与所求，必要时画图；按"设未知数→列方程/关系→求解→检验→规范作答"组织。
- 步骤编号清晰（① ② ③），关键公式单独成行，最终答案用"答：..."结尾。
- 计算后复核：代入原题检验、检查单位与符号（负数、无解、增根）。
- 涉及高考考点时点明方法（坐标法、待定系数法、设而不求、点差法、换元法），并提示易错点。

## 五、数学正确性保障（务必执行）

数学内容容错率低，任何数值错误都会损害可信度。生成含数值的题解/模型时：
1. **逐题验算**：解完方程代入回代；三角形/几何题检查边长满足三角不等式。
2. **公式核对**：对照教材/权威来源确认（椭圆 a²=b²+c²，双曲线 c²=a²+b²，抛物线 y²=2px 焦点 (p/2,0)）。
3. **慎用记忆值**：不确定的数值必须显式推导或标注"示例值"。
4. **模型结果检验**：结果代入模型自洽，量纲一致；数值解与解析解互相印证；给出误差指标。
5. 若一处算错，检查同文档内依赖它的所有后续结果是否需连带修正。

## 六、公式的跨媒介表示对照

| 媒介 | 示例 |
|---|---|
| Markdown | `$\frac{-b\pm\sqrt{b^2-4ac}}{2a}$` |
| LaTeX | `\frac{-b\pm\sqrt{b^2-4ac}}{2a}` |
| Word 文本(Unicode) | (−b±√(b²−4ac))/(2a) |
| 纯文本 | (-b±sqrt(b^2-4ac))/(2a) |

## 七、交付前检查清单

**专题文档**
- [ ] 数学推导正确、数值已验算
- [ ] 与教材定义一致、不超纲（面向基础学生）
- [ ] 结构完整（标题/知识梳理/专题/考点/例题/实战/答案/拓展）
- [ ] 例题编号连续、每考点实战=5、答案齐全
- [ ] 中文排版正确、无公式错漏

**建模论文（官方标准）**
- [ ] 结构含摘要/关键词/重述/分析/假设/符号/建模求解/检验/评价/参考文献/附录
- [ ] 摘要≤1 页，含模型+方法+关键数值结论；关键词 3~5 个
- [ ] 公式、图、表均编号并在正文引用；参考文献 GB/T 7714
- [ ] 假设可辩护、符号统一、结果有误差/灵敏度分析
- [ ] 附录代码可运行、无密钥；总页数符合限制
